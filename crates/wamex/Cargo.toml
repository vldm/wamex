[package]
name = "wamex"
version = "0.1.0"
edition = "2021"

[dependencies]
async-once-cell = { version = "0.5.3", features = ["std"] }
wamex_macro = { version = "0.1.0", path = "../wamex_macro" }
wamex_loader = { version = "0.1.0", path = "../wamex_loader" }
wamex_types = { version = "0.1.0", path = "../wamex_types" }
pin-project-lite = "0.2.16"

[features]
default = []
