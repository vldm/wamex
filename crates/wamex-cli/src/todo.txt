1. Implement a strategy outside `ModuleEmitState::produce_state` to allow different modes
3. Process "Unnamed" data segments ?
4. Recreate relocations
5. Generate the linking, and relocations sections
7. implement "streaming" code copy during emit, without instructions parsing
8. process only relocation entry specific for section
9. Cleanup exports from sub-modules 
10. Add imports for data symbols.


- Add dep in DepGraph for BoundToPrevious symbol
- impl tests for Structure recovery (partially done)
- Convert relocations with negative appends to relocations to previous symbol
- Correct global relocations to external sub-modules

Roadmap:
1. sub-modules "non-lazy" deps.
1.1 Extract "shared" modules in a separate module
2. wasm-link code reduction
2.1 extend twiggy report for line -> code mapping
3. storage globals -> to local vars.
4. emit symbol info 
5. emit debug info 
6. emit linking info


Document:
1. constant exraction 
2. extended const need in segment allocation
3. start fn generator (aka relocation)
4. export wasm-bindgen imported fns (externref shrims stubs)
5. 


! wasm_bindgen closure currently can be exported (prevent it).
! make is_special_fn outer and check relocs instead of name only.
! experiment with Box::pin(load_inner) to reduce size of extracted modules

