[package]
name = "wamex_types"
version = "0.1.0"
edition = "2024"

[dependencies]

wasmparser = { version = "0.238.0", optional = true }
anyhow = "1.0.82"
wasm-encoder = { version = "0.238.0", optional = true }

[features]
default = []
parser = ["dep:wasmparser"]
encoder = ["dep:wasm-encoder"]
full = ["parser", "encoder"]
